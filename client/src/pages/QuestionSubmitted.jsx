import { withRouter } from "react-router-dom";

function QuestionSubmitted({ setCurrentPage }) {
  return (
    <div>
      <h1>Thanks for your question!</h1>
    </div>
  );
}

export default withRouter(QuestionSubmitted);
